F8
    pulsemixer --toggle-mute && notify-send "Volume" "Mute toggled to $(pulsemixer --get-mute)"

F9
    pulsemixer --set-volume 100  && notify-send "Volume" "Set volume to 100"

F10
    pulsemixer --set-volume 150  && notify-send "Volume" "Set volume to maximum (150)"

F11
    $TERMINAL python3
